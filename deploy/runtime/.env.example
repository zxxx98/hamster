# `./deploy/bootstrap.sh` creates `runtime/.env` with all credentials.
# These are the two optional inputs for a first run. APP_ORIGIN must be HTTP(S).
APP_ORIGIN=http://localhost:24000
APP_PORT=24000
